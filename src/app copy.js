// const express = require('express');
// const app = express()

// const morgan = require('morgan')
// const { default: helmet } = require('helmet');




/// ----------------------------------init middleware
// morgan
// app.use(morgan("dev")) // ngan gon cho dev
// app.use(morgan("combined")) // cho prod
// app.use(morgan("common"))
// app.use(morgan("short"))
// app.use(morgan("tiny"))


// helmet
// app.use(helmet())

/// init db


/// init router

/// init error handlers

// module.exports = app;

